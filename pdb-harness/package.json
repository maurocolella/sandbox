{
  "name": "pdb-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Corpus-scale validation of the structure loaders (PDB today; mmCIF and BinaryCIF next) against the harvested PDB archive.",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:pdb": "vitest run tests/pdb-bulk.spec.ts"
  },
  "dependencies": {
    "pdb-parser": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "vitest": "^1.6.0"
  }
}
